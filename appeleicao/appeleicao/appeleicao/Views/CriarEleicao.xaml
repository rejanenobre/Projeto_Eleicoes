<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="appeleicao.Views.CriarEleicao"
             Title="Criação da Eleição">
    <ContentPage.Content>
        <StackLayout>
            <Entry
                Placeholder="Nome do coordenador da votação"
                TextColor="Pink"/>

            <Entry 
                Text="{Binding NomeVot}"
                 Placeholder="Nome da votação"
                 TextColor="Pink"/>

            <Entry 
                Text="{Binding SenhaVot}"
                 Placeholder="Senha da votação"
                 TextColor="Pink"/>

            <Button 
                Command="{Binding CriarVot}"
                Text="Crie sua votação !"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>